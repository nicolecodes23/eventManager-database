<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Link to shared login/register CSS stylesheet -->
    <link rel="stylesheet" href="/css/login-register.css">
    <title>Organiser Login</title>
</head>

<body>
    <!--main container to center content-->
    <div class="container">
        <main class="main-content">
            <!--header with page title, return btn -->
            <header class="topbar">
                <h1>Organiser Login Page</h1>
                <a href="/">
                    <button class="return-home-btn">
                        Return to Main Home
                    </button>
                </a>
                <section class="avatar">
                    <img src="/assets/Organiser.png" alt="Men in suit">
                </section>
            </header>
            <!-- Login form container -->
            <div class="login-content">
                <!-- Login form -->
                <form action="/organiser/login" method="POST" class="login-form">
                    <h2>Sign in to your account</h2>

                    <div class="form-group">
                        <label for="email">Email address</label>
                        <input type="email" name="email" id="email" required>
                    </div>

                    <div class="form-group">
                        <label for="password">Password</label>
                        <input type="password" name="password" id="password" required>
                    </div>
                    <!-- Display error message if login fails -->
                    <% if (error) { %>
                        <p class="error-message">
                            <%= error %>
                        </p>
                        <% } %>

                            <button type="submit" class="btn">Log In</button>
                </form>
                <!-- Link to register page -->
                <p class="register-link">Don’t have an account? <a href="/organiser/register">Register here.</a></p>
            </div>

        </main>
    </div>
</body>

</html>